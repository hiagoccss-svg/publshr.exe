name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-14
            asset_os: macos
            arch: aarch64
          - os: ubuntu-22.04
            asset_os: linux
            arch: x86_64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode (macOS)
        if: runner.os == 'macOS'
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Install Swift (Linux)
        if: runner.os == 'Linux'
        run: |
          curl -fsSL "https://download.swift.org/swift-5.10.1-release/ubuntu2204/swift-5.10.1-RELEASE/swift-5.10.1-RELEASE-ubuntu22.04.tar.gz" -o /tmp/swift.tar.gz
          sudo mkdir -p /opt/swift
          sudo tar -xzf /tmp/swift.tar.gz -C /opt/swift --strip-components=1
          echo "/opt/swift/usr/bin" >> "$GITHUB_PATH"
          sudo apt-get update -qq
          sudo apt-get install -y -qq libncurses6 libcurl4 libxml2

      - name: Package CLI (Linux)
        if: runner.os == 'Linux'
        working-directory: native/publshr
        run: |
          chmod +x scripts/package-release.sh
          bash scripts/package-release.sh "${GITHUB_REF_NAME#v}"

      - name: Package CLI + Publshr.app (macOS)
        if: runner.os == 'macOS'
        working-directory: native/publshr
        run: |
          chmod +x scripts/package-release.sh scripts/package-mac-app.sh
          bash scripts/package-release.sh "${GITHUB_REF_NAME#v}"
          bash scripts/package-mac-app.sh "${GITHUB_REF_NAME#v}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: publshr-${{ matrix.asset_os }}-${{ matrix.arch }}
          path: native/publshr/dist/*

  publish:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          mapfile -t assets < <(find artifacts \( -name '*.tar.gz' -o -name '*.zip' \) -type f)
          if [[ ${#assets[@]} -eq 0 ]]; then
            echo "No release tarballs found under artifacts/" >&2
            find artifacts -type f >&2 || true
            exit 1
          fi
          gh release create "$GITHUB_REF_NAME" \
            --repo "${{ github.repository }}" \
            --title "publshr $VERSION" \
            --notes "macOS: download Publshr-*-macos.app.zip, unzip, drag Publshr.app to Applications. Or clone repo and run ./install-mac-app.sh. Linux: CLI tarball. App syncs git branch cursor/add-makefile-and-install-4aa6 when online." \
            "${assets[@]}"
