import SwiftUI

enum InstallerTheme {
    static let accent = Color(red: 0.0, green: 0.48, blue: 0.83)
    static let foreground = Color.primary.opacity(0.92)
    static let muted = Color.primary.opacity(0.55)
    static let error = Color(red: 0.85, green: 0.2, blue: 0.2)
}
